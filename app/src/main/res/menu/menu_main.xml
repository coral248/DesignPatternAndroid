<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools" tools:context=".MainActivity">

    <item android:id="@+id/action_ip"
        android:icon="@drawable/action_ip"
        android:title="@string/enter_ip"
        app:showAsAction="ifRoom" />

    <item android:id="@+id/action_tool"
        android:icon="@drawable/action_tool"
        android:title="@string/employee_login"
        app:showAsAction="ifRoom" />

    <item android:id="@+id/action_sync"
        android:icon="@drawable/action_sync_off"
        android:title="@string/alarm"
        app:showAsAction="ifRoom" />

</menu>
